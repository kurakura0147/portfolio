= render 'shared/header'

= image_tag @contents.image
= @contents.name
= @contents.text
= @contents.created_at.strftime("%Y/%m/%d %H:%M")

= link_to edit_content_path do
  編集画面に

= link_to content_path, method: :delete, data: {confirm: "本当に削除しますか？"} do
  削除をする

= render 'shared/footer'
